الفولدر ده فاضي عن قصد.

موديل الـ XGBoost بيتدرب لايف أوتوماتيكيًا أول ما تنادي أي endpoint بتاع
التنبؤ (/api/patient/{id}/predict-ml)، من بيانات patient.fact_patient في
القاعدة مباشرة — مفيش حاجة تحتاج تحطها هنا يدويًا.

بعد أول تدريب، الملف asthma_xgb_model.joblib هيتحفظ هنا تلقائيًا كـ"كاش"
عشان مايتدربش من الصفر تاني في كل مرة تشغّل فيها السيرفر.

لو ضفت مرضى جداد للقاعدة وعايز الموديل ياخد بالهم، اعمل:
POST /api/patient/retrain-model
